class Uporabnik {
    constructor(username, password, is_admin, is_locked) {
        this.username = username;
        this.password = password;
        this.is_admin = is_admin;
        this.is_locked = is_locked;
    }
}